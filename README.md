#### My alacritty config

##### Pre-requisites
- RobotoMono Nerd Font Mono `brew install --cask font-roboto-mono-nerd-font`

##### Installation
```shell
git clone --recurse-submodules --single-branch --branch macos https://github.com/sulo1337/alacritty.git ~/.config/alacritty
```
